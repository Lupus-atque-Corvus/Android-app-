import 'package:flutter/material.dart';

class PeriodCalendarScreen extends StatelessWidget {
  const PeriodCalendarScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('PeriodCalendarScreen')));
}
