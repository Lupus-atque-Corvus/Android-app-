import 'package:flutter/material.dart';

class TraumRadius {
  TraumRadius._();

  static const BorderRadius card = BorderRadius.all(Radius.circular(20));
  static const BorderRadius cardLarge = BorderRadius.all(Radius.circular(24));
  static const BorderRadius button = BorderRadius.all(Radius.circular(50));
  static const BorderRadius inputField = BorderRadius.all(Radius.circular(14));
  static const BorderRadius chip = BorderRadius.all(Radius.circular(50));
  static const BorderRadius bottomNav =
      BorderRadius.vertical(top: Radius.circular(20));
  static const BorderRadius badge = BorderRadius.all(Radius.circular(8));
  static const BorderRadius small = BorderRadius.all(Radius.circular(8));
}
